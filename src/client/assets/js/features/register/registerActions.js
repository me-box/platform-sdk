//import {register} from 'app/store/configureStore';

export function registerReducer(store, id, reducer) {
	console.log("need to register this reducer somehow!");
	//register(store, id, scopeify(id, reducer));
}